# Streamlit Food Demand Forecasting App

Run from this folder:

```powershell
streamlit run streamlit_app.py
```

The app loads the trained XGBoost model and encoder from `../models`, then uses the processed training data from `../data/processed/train_merged.csv`.
